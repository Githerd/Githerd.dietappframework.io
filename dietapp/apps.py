from django.apps import AppConfig


class DietappConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'dietapp'
    
app.listen(port, () => {
  console.log(`dietapp listening on port ${port}`)
})
